# BugBot rules

If a changed line contains the exact string `TODO: Remove me before merging`:

- Leave an **inline** review comment on that line (not just a top-level summary).
- Message: "Remove this demo TODO before merging."
